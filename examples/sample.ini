[DEFAULT]
str_search = search word
str_replace = replace word
opt_re = re.UNICODE | re.IGNORECASE | re.MULTILINE | re.DOTALL

[opt_replace]
str_search = search word
str_replace = replace word
opt_re = re.UNICODE | re.IGNORECASE | re.MULTILINE | re.DOTALL

